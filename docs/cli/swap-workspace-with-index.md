# swap-workspace-with-index

```
Exchange the focused workspace's position with the workspace at the specified position

Usage: komorebic.exe swap-workspace-with-index <TARGET>

Arguments:
  <TARGET>
          Target index (zero-indexed)

Options:
  -h, --help
          Print help

```
