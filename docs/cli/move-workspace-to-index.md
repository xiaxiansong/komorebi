# move-workspace-to-index

```
Move the focused workspace to the specified position on its monitor

Usage: komorebic.exe move-workspace-to-index <TARGET>

Arguments:
  <TARGET>
          Target index (zero-indexed)

Options:
  -h, --help
          Print help

```
