# cycle-window-history

```
Walk the focused container's window focus history, raising the window walked to

Usage: komorebic.exe cycle-window-history <CYCLE_DIRECTION>

Arguments:
  <CYCLE_DIRECTION>
          [possible values: previous, next]

Options:
  -h, --help
          Print help

```
